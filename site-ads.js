(function(){
  if(!window.HC_PORTAL_CONFIG?.appsScriptUrl)return;
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  async function role(){try{if(!window.HCAuth?.auth)return 'public';const u=HCAuth.auth.currentUser;if(!u)return 'public';return (await HCAuth.api('getProfile')).profile?.role||'public'}catch{return 'public'}}
  function seenKey(id){return 'hcAdSeen:'+id+':'+new Date().toISOString().slice(0,10)}
  async function load(){try{const fd=new FormData();fd.set('action','publicAds');fd.set('page',location.pathname.split('/').pop()||'home.html');fd.set('role',await role());const r=await fetch(HC_PORTAL_CONFIG.appsScriptUrl,{method:'POST',body:fd,cache:'no-store'}),d=await r.json();(d.ads||[]).forEach(render)}catch(_){} }
  function render(a){const cap=Math.max(0,Number(a.frequencyCap)||0),seen=Number(localStorage.getItem(seenKey(a.id))||0);if(cap&&seen>=cap)return;const el=document.createElement('aside');el.className='hc-site-ad '+String(a.textEffect||'').toLowerCase().replace(/\s+/g,'-');el.dataset.placement=a.placement||'top';let media='';if(a.mediaUrl)media=a.contentType==='Video'?`<video controls playsinline preload="metadata" src="${esc(a.mediaUrl)}"></video>`:`<img src="${esc(a.mediaUrl)}" alt="${esc(a.headline||'Promotion')}">`;el.innerHTML=`${a.dismissible?'<button class="hc-ad-close" type="button" aria-label="Close promotion">×</button>':''}${media}<div><strong>${esc(a.headline||'')}</strong><p>${esc(a.message||'')}</p>${a.ctaUrl?`<a class="btn primary small" href="${esc(a.ctaUrl)}">${esc(a.ctaText||'Know More')}</a>`:''}</div>`;el.querySelector('.hc-ad-close')?.addEventListener('click',()=>el.remove());if(a.placement==='bottom'){el.classList.add('ad-bottom');document.body.appendChild(el)}else if(a.placement==='dashboard'){document.querySelector('.portal-main .container')?.prepend(el)}else{document.body.insertBefore(el,document.body.firstChild)};localStorage.setItem(seenKey(a.id),String(seen+1))}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',load):load();
})();