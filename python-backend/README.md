# Optional Python backend

This is a future FastAPI service scaffold. GitHub Pages cannot execute Python, so the production site continues to use Firebase Authentication + Google Apps Script unless this service is separately hosted (for example Cloud Run, Render, Railway, Fly.io or a VPS). Do not put secrets in the browser or GitHub repository.

Use this service later for CPU-heavy research, caching, reporting or AI integrations after deploying it behind HTTPS.
